package PackageExamples;
/// Here we are practice what are the package.how we can use the packages in the import syntax
import java.lang.Math;
public class paca {
    public static void main(String[] args) {
                    int a =300;
                    int b=200;
                    int max=0;
                    max=Math.max(a,b);
                    System.out.println("Enter the the value " + max);
                }
            }

